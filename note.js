// #eee是红色，#ddd是灰色适合做bg,#fff是白色，#000是黑色
// 引入jQuery有两种方法，一是将jQuery的代码放入src中，二是用bootcdn
// 引入jQuery后，就可以用jquery获取到元素，进行事件监听
// ${}这是js的插值语法，{}中接受变量，实现动态获取
// 当返回第一个网页时，之前添加的网站都会消失，解决方法是使用数据结构中的数组，数组中存的是hash。只要知道该新增网页的hash，下次
// 进入重新渲染即可
//如果启用了parcel，那么代码js代码外部会默认生成一个作用域，从而再声明的全局变量获取不到，用window.变量 = {}可以解决
// <!-- a标签比较特殊，a标签作为内联元素，却可以包块级元素 -->
// 当用户点勾选浏览器设置中的清楚cookie及其他网站数据后就会清理掉localStorage
//如何删除添加的导航窗格
//平均布局用负margin
// parcel build src/index.html该命令打包文件，
